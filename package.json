{
  "name": "npm-safe-dsh",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": { "node": ">=22.19" },
  "scripts": { "typecheck": "pnpm -r run typecheck", "build": "pnpm -r run build", "test": "pnpm -r run test" },
  "devDependencies": { "@npm-safe/core": "workspace:^" }
}
